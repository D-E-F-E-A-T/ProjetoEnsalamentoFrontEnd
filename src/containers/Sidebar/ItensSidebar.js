import React from "react";
import { Apartment as ApartmentIcon } from "@material-ui/icons";

const Items = [
  { id: 0, label: "Dashboard", link: "/predios", icon: <ApartmentIcon /> }
];

export default Items;
